import { ArrowUpOutlined, ArrowDownOutlined } from '@ant-design/icons';
import styled from 'styled-components';

export const Wrapper = styled.div`
  &:hover {
    cursor: pointer;
  }
`;

export const ArrowDownOutlinedIcon = styled(ArrowDownOutlined)`
  font-size: 16px;
  margin-left: 0.25rem;
`;

export const ArrowUpOutlinedIcon = styled(ArrowUpOutlined)`
  font-size: 16px;
  margin-left: 0.25rem;
`;
